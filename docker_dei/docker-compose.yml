version: '3.8'

services:
  web:
    build:
      context: web
      dockerfile: ./Dockerfile
    ports:
      - "8080:8080"

    environment:
      SPRING_PROFILES_ACTIVE: production


volumes:
  spring-data: