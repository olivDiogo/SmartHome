@startuml
'https://plantuml.com/sequence-diagram
!pragma layout smetana

autonumber

actor "User" as User
participant "RestClient" as RC
box "Smart Home"
    participant "Server" as Server
    participant "Database" as DB
end box

User -> RC : I want to get the list of all devices in a room
activate RC

RC -> Server : getDevicesByRoomID(roomID)
activate Server

Server -> DB : findByRoomID(roomID)
activate DB

DB --> Server : deviceDataModelList
deactivate DB

Server --> RC : deviceDTOList
deactivate Server

RC --> User : deviceDTOList
deactivate RC

@endumll